# Task G: 사건 조회 페이지 구현 + 이산계정 권한 분리
# 목표:
#   1. middleware.ts — 이산계정은 /cases-view만 접근, 나머지 전부 차단
#   2. app/cases-view/page.tsx — 사건 조회 페이지 전체 구현
#      - 전체 조회 탭: 모든 사건 목록 (이산계정은 기본값)
#      - 담당자별 조회 탭: 로그인 계정 = 영업담당자(salesManagerId)인 사건만 표시
#   3. /api/cases-view 엔드포인트 — 권한별 데이터 분리

---

## 사전 확인

아래 파일 읽어서 현재 상태 파악:
- middleware.ts (전체)
- app/api/cases/route.ts (기존 사건 조회 API 구조 참고용)
- prisma/schema.prisma (Case, Patient 모델 필드 확인)

---

## STEP 1: middleware.ts — 이산계정 라우트 제한

기존 middleware.ts를 읽은 후, 이산계정 제한 로직을 수술적으로 추가.
기존 /admin 제한 로직은 건드리지 말 것.

기존 권한 체크 블록 안에 아래 로직 추가:

```typescript
// 이산계정: /cases-view와 /api/cases-view만 허용, 나머지 차단
if (session?.user?.role === '이산계정') {
  const allowed = ['/cases-view', '/api/cases-view', '/login', '/api/auth']
  const isAllowed = allowed.some(path => req.nextUrl.pathname.startsWith(path))
  if (!isAllowed) {
    return NextResponse.redirect(new URL('/cases-view', req.url))
  }
}
```

---

## STEP 2: /api/cases-view 엔드포인트 생성

파일: app/api/cases-view/route.ts (신규 생성)

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { auth } from '@/auth'
import { prisma } from '@/lib/prisma'

export async function GET(req: NextRequest) {
  const session = await auth()
  if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const { searchParams } = new URL(req.url)
  const myOnly = searchParams.get('myOnly') === 'true'  // 담당자별 조회 탭
  const search = searchParams.get('search') ?? ''        // 성명 검색
  const caseType = searchParams.get('caseType') ?? ''    // 상병 필터
  const status = searchParams.get('status') ?? ''        // 진행상황 필터

  const userId = session.user.id
  const role = session.user.role

  // 담당자별 조회: myOnly=true이거나 이산계정이 아닌 경우 본인 영업사건만
  // 전체 조회: 이산계정은 항상 전체, 다른 계정은 myOnly=false일 때 전체
  const salesFilter = myOnly ? { salesManagerId: userId } : {}

  const cases = await prisma.case.findMany({
    where: {
      ...salesFilter,
      ...(search ? { patient: { name: { contains: search } } } : {}),
      ...(caseType ? { caseType } : {}),
      ...(status ? { status } : {}),
    },
    include: {
      patient: {
        select: {
          name: true,
          ssn: true,
          phone: true,
          address: true,
        },
      },
      salesManager: { select: { name: true } },
      caseManager: { select: { name: true } },
    },
    orderBy: { createdAt: 'desc' },
    take: 200,
  })

  // 이산계정: 주민번호 마스킹, 상세 의료정보 제외
  const result = cases.map(c => ({
    id: c.id,
    caseType: c.caseType,
    status: c.status,
    patientName: c.patient?.name ?? '',
    ssn: role === '이산계정'
      ? (c.patient?.ssn ?? '').replace(/(\d{6})-?(\d{7})/, '$1-*******')
      : (c.patient?.ssn ?? ''),
    phone: c.patient?.phone ?? '',
    address: c.patient?.address ?? '',
    salesManager: c.salesManager?.name ?? '',
    caseManager: c.caseManager?.name ?? '',
    createdAt: c.createdAt,
  }))

  return NextResponse.json(result)
}
```

---

## STEP 3: app/cases-view/page.tsx 전체 구현

파일: app/cases-view/page.tsx
현재 Placeholder만 있으므로 전체 교체.

```tsx
'use client'

import { useEffect, useState } from 'react'
import { useSession } from 'next-auth/react'

// 상태값 한글 매핑
const STATUS_MAP: Record<string, string> = {
  CONSULTING: '상담 중',
  CONTRACTED: '약정 완료',
  DOC_COLLECTING: '자료 수집 중',
  SUBMITTED: '공단 접수 완료',
  EXAM_REQUESTED: '특진진찰요구서 수령',
  EXAM_CLINIC_SELECTED: '특진병원 선택 완료',
  EXAM_SCHEDULED: '특진 일정 확정',
  IN_EXAM: '특진 진행 중',
  EXAM_DONE: '특진 완료',
  EXPERT_REQUESTED: '전문조사요구서 수령',
  EXPERT_DONE: '전문조사 완료',
  DECISION_RECEIVED: '결정통지서 수령',
  REVIEWING: '검토 중',
  APPROVED: '승인',
  REJECTED: '불승인',
  CLOSED: '종결',
  OBJECTION: '이의제기 진행 중',
  WAGE_CORRECTION: '평균임금 정정',
}

const CASE_TYPE_MAP: Record<string, string> = {
  HEARING_LOSS: '소음성 난청',
  COPD: 'COPD',
  PNEUMOCONIOSIS: '진폐',
  MUSCULOSKELETAL: '근골격계',
  OCCUPATIONAL_ACCIDENT: '업무상 사고',
  OCCUPATIONAL_CANCER: '직업성 암',
  BEREAVED: '유족',
  OTHER: '기타',
}

export default function CasesViewPage() {
  const { data: session } = useSession()
  const [activeTab, setActiveTab] = useState<'all' | 'my'>('all')
  const [cases, setCases] = useState<any[]>([])
  const [loading, setLoading] = useState(true)
  const [search, setSearch] = useState('')
  const [caseType, setCaseType] = useState('')
  const [status, setStatus] = useState('')

  const role = session?.user?.role ?? ''
  const isIsanAccount = role === '이산계정'

  const fetchCases = async (myOnly: boolean) => {
    setLoading(true)
    try {
      const params = new URLSearchParams()
      if (myOnly) params.set('myOnly', 'true')
      if (search) params.set('search', search)
      if (caseType) params.set('caseType', caseType)
      if (status) params.set('status', status)

      const res = await fetch(`/api/cases-view?${params.toString()}`)
      if (res.ok) {
        const data = await res.json()
        setCases(data)
      }
    } catch (e) {
      console.error('사건 조회 실패', e)
    } finally {
      setLoading(false)
    }
  }

  useEffect(() => {
    fetchCases(activeTab === 'my')
  }, [activeTab])

  const handleSearch = () => {
    fetchCases(activeTab === 'my')
  }

  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-6" style={{ color: '#006838' }}>
        사건 조회
      </h1>

      {/* 탭 — 이산계정은 탭 숨김 (전체 조회만) */}
      {!isIsanAccount && (
        <div className="flex gap-2 mb-6">
          {[
            { key: 'all', label: '전체 조회' },
            { key: 'my', label: '내 담당 사건' },
          ].map(tab => (
            <button
              key={tab.key}
              onClick={() => setActiveTab(tab.key as 'all' | 'my')}
              className="px-4 py-2 rounded text-sm font-medium"
              style={{
                backgroundColor: activeTab === tab.key ? '#29ABE2' : '#f1f5f9',
                color: activeTab === tab.key ? 'white' : '#64748b',
              }}
            >
              {tab.label}
            </button>
          ))}
        </div>
      )}

      {/* 필터 영역 */}
      <div className="flex gap-3 mb-4 flex-wrap">
        <input
          type="text"
          placeholder="성명 검색"
          value={search}
          onChange={e => setSearch(e.target.value)}
          onKeyDown={e => e.key === 'Enter' && handleSearch()}
          className="border px-3 py-2 rounded text-sm"
          style={{ borderColor: '#CBD5E1' }}
        />
        <select
          value={caseType}
          onChange={e => setCaseType(e.target.value)}
          className="border px-3 py-2 rounded text-sm"
          style={{ borderColor: '#CBD5E1' }}
        >
          <option value="">전체 상병</option>
          {Object.entries(CASE_TYPE_MAP).map(([k, v]) => (
            <option key={k} value={k}>{v}</option>
          ))}
        </select>
        <select
          value={status}
          onChange={e => setStatus(e.target.value)}
          className="border px-3 py-2 rounded text-sm"
          style={{ borderColor: '#CBD5E1' }}
        >
          <option value="">전체 진행상황</option>
          {Object.entries(STATUS_MAP).map(([k, v]) => (
            <option key={k} value={k}>{v}</option>
          ))}
        </select>
        <button
          onClick={handleSearch}
          className="px-4 py-2 rounded text-sm text-white"
          style={{ backgroundColor: '#29ABE2' }}
        >
          조회
        </button>
      </div>

      {/* 결과 카운트 */}
      <div className="text-sm text-gray-500 mb-3">
        총 {cases.length}건
        {activeTab === 'my' && !isIsanAccount && ' (내 영업 담당 사건)'}
      </div>

      {/* 테이블 */}
      {loading ? (
        <div className="text-gray-400 py-8 text-center">불러오는 중...</div>
      ) : (
        <div className="overflow-x-auto">
          <table className="w-full text-sm border-collapse">
            <thead>
              <tr style={{ backgroundColor: '#006838', color: 'white' }}>
                <th className="px-3 py-2 text-left">성명</th>
                <th className="px-3 py-2 text-left">주민번호</th>
                <th className="px-3 py-2 text-left">연락처</th>
                <th className="px-3 py-2 text-left">주소</th>
                <th className="px-3 py-2 text-left">상병</th>
                <th className="px-3 py-2 text-left">진행상황</th>
                <th className="px-3 py-2 text-left">영업담당</th>
                <th className="px-3 py-2 text-left">사건담당</th>
              </tr>
            </thead>
            <tbody>
              {cases.length === 0 ? (
                <tr>
                  <td colSpan={8} className="text-center py-8 text-gray-400">
                    조회 결과가 없습니다.
                  </td>
                </tr>
              ) : (
                cases.map((c, i) => (
                  <tr
                    key={c.id}
                    style={{ backgroundColor: i % 2 === 0 ? 'white' : '#f8fafc' }}
                  >
                    <td className="px-3 py-2 font-medium">{c.patientName}</td>
                    <td className="px-3 py-2 text-gray-600">{c.ssn}</td>
                    <td className="px-3 py-2">{c.phone}</td>
                    <td className="px-3 py-2 text-gray-600 max-w-xs truncate">{c.address}</td>
                    <td className="px-3 py-2">
                      <span
                        className="px-2 py-1 rounded text-xs"
                        style={{ backgroundColor: '#e0f2fe', color: '#0369a1' }}
                      >
                        {CASE_TYPE_MAP[c.caseType] ?? c.caseType}
                      </span>
                    </td>
                    <td className="px-3 py-2">
                      <span
                        className="px-2 py-1 rounded text-xs"
                        style={{
                          backgroundColor: c.status === 'APPROVED' ? '#dcfce7'
                            : c.status === 'REJECTED' ? '#fee2e2'
                            : '#f1f5f9',
                          color: c.status === 'APPROVED' ? '#166534'
                            : c.status === 'REJECTED' ? '#991b1b'
                            : '#475569',
                        }}
                      >
                        {STATUS_MAP[c.status] ?? c.status}
                      </span>
                    </td>
                    <td className="px-3 py-2">{c.salesManager}</td>
                    <td className="px-3 py-2">{c.caseManager}</td>
                  </tr>
                ))
              )}
            </tbody>
          </table>
        </div>
      )}
    </div>
  )
}
```

---

## STEP 4: 사이드바에서 이산계정 메뉴 제한

사이드바 컴포넌트 파일을 찾아서 (components/Sidebar.tsx 또는 유사 파일),
이산계정일 때 사건 조회 메뉴만 표시되도록 수정.

기존 메뉴 렌더링 부분에서:
```tsx
// 이산계정은 사건 조회만 표시
const visibleMenus = role === '이산계정'
  ? menus.filter(m => m.href === '/cases-view')
  : menus
```

사이드바 파일 구조에 맞게 수술적으로 적용.
전체 재작성 금지 — 기존 메뉴 배열/렌더링 방식 그대로 유지하면서 필터만 추가.

---

## 완료 후 확인
- [ ] 타입 에러 없음
- [ ] middleware.ts에 이산계정 라우트 제한 추가됨
- [ ] /cases-view 페이지 — 전체 조회 탭 동작
- [ ] /cases-view 페이지 — 내 담당 사건 탭 (salesManagerId 기준 필터)
- [ ] 이산계정 로그인 시 주민번호 마스킹 확인
- [ ] 이산계정 로그인 시 /cases 접근 시 /cases-view로 리다이렉트
- [ ] git add . && git commit -m "feat: 사건 조회 페이지 구현 + 이산계정 권한 분리" && git push
