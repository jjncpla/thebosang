# TBSS 기획기획서

> **담당 에이전트**: 기획
> **버전**: v3.1
> **최종수정**: 2026.04.17
> **목적**: Phase 로드맵 상세, PRD, 신규 기능 구상, 미결 사항 추적

---

## 1. Phase별 상세 체크리스트

> **핵심 원칙**: 데이터 싱크로나이징 우선. 스키마/필드 통일이 모든 기능의 선행 조건.
> **개발 시간**: 하루 평균 4시간 (기존 2시간 → 상향)

### Phase 1 — 기반 정비 ✅ 완료 (2026.03)

- [x] PDF 생성 타입 에러 수정 (pdf-lib 전환)
- [x] 페이지명 일괄 변경 (To Do List 등)
- [x] 로그인 페이지 개선 (인사말/공지 박스, 가입 요청 버튼)
- [x] 관리자 페이지 기본 구성 (계정 관리, 공지사항 작성)
- [ ] ~~전체 페이지 퀵 링크 패널~~ → Phase 3-B로 이동 (우선순위 낮음)

---

### Phase 2 — 사건 관리 핵심 구조 ✅ 대부분 완료 (2026.03~04)

- [x] 사건관리 스키마 설계 (PRD 기반 Patient → Case → HearingLossDetail → HearingLossExam)
- [x] 소음성 난청 사건 상세 페이지 (특진 n차 동적 추가, 검사 수치, 전문조사, 결정 단계)
- [x] 상담 관리 목록 (상담중/약정/보류 상태, 필터)
- [x] 사건 목록 필터링 (상병, 진행단계, 담당자, TF, 지사별)
- [x] 보험급여 청구서식 좌표 기반 PDF 오버레이 시스템 구축 (7개 서식 좌표 확정)
- [x] 사건 조회 페이지 `/cases-view` (이산 계정 한정)
- [x] 직업력 자동 산정 기본 구현 (Claude API, 상용직/일용직 구분, 20일=1개월 환산)
- [x] 사이드바/UI 개선 (서브메뉴 가독성, 최초특진 n차 동적 추가)

### Phase 2+ — 지사장 관리 페이지 ✅ 대부분 완료 (2026.03~04)

- [x] 운영규정·취업규칙 열람 (비용/복지/인사/취업규칙, 인라인 편집, 검색 하이라이트)
- [x] 회의록 관리 (6개 카테고리, CRUD, 첨부파일)
- [x] 법인 실적 관리 — 영업 약정건수 (월별 테이블, 12개 질병유형 컬럼, 분기/연간 집계)
- [x] 법인 실적 관리 — 정산내역 (xlsx import, 분할납부, 지사별 모니터링)
- [ ] 인사평가 → Phase 5로 이동 (올해 3분기 중 구현, 시상식 평가 기준 PDF 보유)

### Phase 2+ — TF 특진 일정 ✅ 완료 (2026.04.07)

- [x] `/tf/special-clinic` 전체 구현
- [x] 텔레그램 봇(고독한 일정방) webhook → 파서 → PostgreSQL → 캘린더 UI
- [x] 월별/주별 뷰, TF별 색상, 접기/펼치기, 말풍선 팝업, 공휴일(공공데이터API), 드래그 이동, 수동입력/텍스트 파싱

---

### 의존관계 흐름

```
Phase 3-A: 스키마 통일 + TF 샘플 E2E 테스트
   ↓
Phase 3-B: 사건 데이터 파이프라인 검증 (상담→결정→처분검토→이의제기)
   ↓
Phase 3-C: TO-DO LIST 실연동 + 자동 서식 DB 연동
   ↓
Phase 4: 대량 데이터 이관 + 텔레그램 확장
   ↓
Phase 5: 독립 기능 개선 (직력 신뢰도, 평균임금 로직, 인사평가)
   ↓
Phase 6: RAG 시스템 (장기)
```

---

### Phase 3-A — 엑셀 임포트 + E2E 데이터 파이프라인 ✅ 완료 (2026.04)

> **목표**: TF 엑셀 데이터를 정확히 임포트하고, 스키마/필드의 "정답지"를 확정한다.

#### 3-A-1. TF 엑셀 임포트 매핑 로직 완성

- [x] 엑셀 진행경과 필드 매핑 — CaseStatus Enum 매핑 확정, 클라이언트 사이드 파싱 (BATCH_SIZE=50)
- [x] 엑셀 컬럼 ↔ CaseStatus Enum 매핑 테이블 확정 (`lib/constants/case.ts`)
- [x] 엑셀 데이터 검증 로직 고도화 — `/cases/import`, `/admin/import` 페이지 구현
- [x] Patient ↔ Case ↔ HearingLossDetail 3단계 트랜잭션 정합성 확인 — 상병별 임포트 API 분리 (`/api/import/hearing-loss`, `/api/import/copd`, `/api/import/pneumoconiosis`)
- [x] 담당자 필드 User FK 연결 — ContactSelector 컴포넌트로 name+mobile+userId 반환, `map-case-managers` API 자동매핑

#### 3-A-2. 스키마/필드 최종 확정

- [x] schema.prisma 최종 확정 — 38개 모델 (auth.prisma 3개 별도, 총 41개)
- [x] 임포트 전용 엑셀 양식 구조 확정 — 상병별(소음성난청/COPD/진폐) 시트 분리
- [x] 임포트 API 컬럼 매핑 양식 기준 확정
- [x] 상담 임포트 API 추가 (`/api/admin/import/consultation`)

#### 3-A-3. E2E 데이터 흐름 관통 테스트

- [x] 상담→약정→접수→특진→전문조사→결정 전체 흐름 검증 — `lib/status-transition.ts` 자동 상태 전이 구현
- [x] 상태 변경 시 사건 목록 필터·상담관리 목록·타임라인 자동 반영 확인
- [x] 특진 일정 캘린더 ↔ 사건 상세 연동 검증
- [x] FK 연결 정합성 확인 — Contact→User, Case→salesManager/caseManager 연결 완료

---

### Phase 3-B — 처분검토·이의제기 데이터 연동 ✅ 완료 (2026.04)

> **목표**: 결정 이후 단계(처분검토 → 이의제기/종결/평임정정)의 데이터 파이프라인을 완성한다.

#### 3-B-1. 처분검토 페이지 DB 연동

- [x] DECISION_RECEIVED 사건 자동 인입 — `/objection/review` 페이지 + `/api/objection/review` API
- [x] 검토 결과 입력 → Case 상태 자동 전이 — ObjectionReview 모델, 상태 전이 연동
- [x] 불승인 사유 입력 + 이의제기 결정 연동 — ObjectionCase 자동 생성 (progressStatus '접수대기')
- [x] 정보공개청구 흐름 구현 — INFO_REQUESTED 상태, 정보공개청구서 서식 포함
- [x] 평균임금 정정 — `/objection/wage-review/[id]` 페이지, WageReviewData 모델

#### 3-B-2. 이의제기 기일관리 ↔ 사건 DB 연동

- [x] 기일관리 TF 분류 매핑 — TF 필터, `init-normalize-objection-tf` 마이그레이션
- [x] OBJECTION 선택 → 기일관리 자동 인입 — ObjectionCase caseId FK 연결
- [x] 제척기간 자동 산정 — `/objection/deadline` 페이지, 90일/180일 카운트다운
- [x] 제척 임박 → TODO 자동 생성 — `/api/cron/objection-deadline` 크론잡 구현
- [x] 공통 필드 사건 DB 자동 로딩 — Case→ObjectionReview→ObjectionCase B방식 FK

#### 3-B-3. 이유서·의견서 서식 + 기타

- [x] 이유서 작성 화면 — `/objection-write` 페이지, 사건 정보 자동 반영
- [x] 이유서 PDF 생성 — `/api/objection/cases/[id]/generate-reason`, `/api/objection/cases/[id]/generate-form`
- [x] 사건 조회 이산계정 권한 검증 — `/cases-view` + `/api/cases-view` API
- [x] 이의제기 임포트 — `/api/admin/import/objection-case`, `/api/admin/import/objection-review`

---

### Phase 3-C — TO-DO LIST 실연동 + 자동 서식 DB 연동 ✅ 완료 (2026.04)

> **목표**: TO-DO LIST가 실제 업무 허브로 동작하고, 서식이 DB 데이터로 자동 생성된다.

#### 3-C-1. TO-DO LIST 자동 연동

- [x] 처분검토·제척 임박·정산 요청 → TODO 자동 생성 — `/api/todos` + `/api/todos/stats` API
- [x] TODO 유형별 필터링 + 완료/미완료 상태 관리 — `/todo` 페이지 달력 그리드 + DB 연동
- [x] TODO 수행율 집계 (관리자용 KPI) — `/api/todos/stats`
- [x] 자유 기술 메모 공간 — 개인 메모 DB 저장

#### 3-C-2. 자동 서식 DB 연동 + 확장

- [x] 서식 7종 → **33종으로 대폭 확장** (공란 18종 + 기입 15종)
  - 추가: 공인노무사업무처리부(4종), 요양급여, 휴업급여, 정보공개청구서, 위임장, 연금선택(2종), 유족급여(2종), 이직자건강진단, 분진확인서 등
- [x] 좌표 에디터 통합 — DB 저장/로드 기능 (`/api/forms/coordinates`, SystemConfig 모델)
- [x] 테스트 PDF 생성 패널 — `/api/forms/test-generate`
- [x] 사건 상세 → 서식 생성 버튼 → DB 데이터 자동 완성 → PDF 다운로드 흐름 완성
- [x] 서식 좌표 DB 저장 — 배포 없이 좌표 즉시 반영 가능
- [ ] ssn8~13, clinic2/3 좌표 일부 미확정 (현재 0,0) → Phase 4-3 안정화에서 처리
- [ ] 도장 이미지 오버레이 — 미구현 (Phase 5로 이동)

---

### Phase 4 — 대량 데이터 이관 + 텔레그램 확장 + 안정화 (2026.04~)

> **목표**: 전국 TF 데이터를 일괄 이관하고, 텔레그램 연동을 확장하여 실무 투입한다.

#### 4-1. 전국 TF 데이터 일괄 임포트 ✅ 완료 (2026.04)

- [x] 상병별 임포트 API 구현 — `/api/import/hearing-loss`, `/api/import/copd`, `/api/import/pneumoconiosis`
- [x] 클라이언트 사이드 Excel 파싱 (BATCH_SIZE=50 JSON 배치, 서버 OOM 방지)
- [x] 관리자 임포트 UI — `/admin/import` + `/cases/import` 페이지
- [x] 상담·이의제기 임포트 — `/api/admin/import/consultation`, `objection-case`, `objection-review`, `wage-review`
- [x] 임포트 이력 로깅 + 중복 감지
- [x] TF명 정규화 마이그레이션 — `init-normalize-tf`, `init-fix-tf-name` API

#### 4-2. 텔레그램 확장 + 통합 캘린더 🟡 부분 완료

- [x] **담당TF 모니터링 페이지** — `/tf-monitor` 페이지, TF별 메시지 일자별 나열
- [x] 텔레그램 webhook 수신 + 파서 — `/api/telegram/webhook`, `/api/webhook/telegram`, TelegramMessage 모델
- [x] 특진 일정 캘린더 — `/tf/special-clinic` (월별/주별 뷰, TF 색상, 공휴일 API 연동)
- [x] 텔레그램 알림 발송 — `/api/telegram/notify`, `/api/telegram/cron-notify`
- [x] 텔레그램 메시지 파이프라인 스키마 — TelegramChatroom/TelegramEvent 모델, `/api/init-telegram-events` (Step 1)
- [x] 텔레그램 채팅방 관리 페이지 — `/admin/telegram-chatrooms` CRUD + DELETE API (Step 2)
- [ ] 텔레그램 메시지 → 사건 DB 자동 매칭 (키워드 감지) — 미구현
- [ ] 통합 캘린더 확장 (질판위/외근/영업 일정 추가) — 미구현

#### 4-3. 실무 피드백 + 안정화 🟡 일부 진행

- [x] 비밀번호 정책 및 변경 기능 — `/mypage/password`, `/api/user/password`, `lib/password-policy.ts`
- [x] 권한·네비게이션 정비 — 미사용 placeholder 페이지 제거 (`/issues`, `/tf-notice`, `/tf-stats`), 내 담당사건 필터 추가
- [x] 서식 SSN 13자리 + 날짜 8자리 분할 필드 추가 — 의료급여/상병/유족 서식 대응
- [x] 서식 FORM_FIELDS ↔ DB 좌표 머지 수정 + 의료급여 PDF 템플릿 교체
- [ ] 알파 테스트: 울산지사 내근직 2~3명 대상 실사용 테스트
- [ ] 피드백 수렴 및 UI/UX 개선 반영
- [ ] 버그 수정 + 성능 최적화 (대량 데이터 로딩 시 페이지네이션, 검색 속도)
- [ ] 미확정 좌표 확정 (ssn8~13, clinic2/3)
- [ ] 법령 및 규정 페이지 정비 (`/law` placeholder)

**Phase 4 완료 기준**:
- 전국 TF 소음성 난청 사건 데이터가 TBSS에 이관 완료
- 텔레그램 메시지가 사건 DB와 연동되어 모니터링 가능
- 울산지사 기준 실무 투입 가능 수준의 안정성 확보

---

### Phase 5 — 독립 기능 개선 + 확장 기획 (2026.10 ~ 12)

> **목표**: 개별 기능의 완성도를 높이고, 타 상병 확장을 기획한다.
> **예상 소요**: 약 10~12주 (총 200~240시간)
> **선행 조건**: Phase 4 완료

#### 5-1. 직업력 자동 산정 신뢰도 개선 (3주, ~60시간)

- [ ] 일용직 1개월 미만 산출 로직 재검증
- [ ] Claude API 프롬프트 최적화 (신뢰도 95%+ 목표)
- [ ] 검증 데이터셋 구축: 수기 산정 결과 vs API 산정 결과 비교표 (최소 30건)
- [ ] 산출 결과에 신뢰도 점수 표시 + 수동 교정 UI
- [ ] 타 상병 공통 적용 가능 구조로 리팩터링

#### 5-2. 평균임금 내부 로직 완성 (2주, ~40시간)

- [ ] 고령자 휴업급여 감액 규정 반영
- [ ] 평균임금 증감에 관한 특례 로직 구현
- [ ] 소비자물가변동률 적용 여부 판단 로직
- [ ] 기존 평균임금 증감 테이블 엑셀 로직과 결과 비교 검증

#### 5-3. 인사평가 — 연말 시상식 대상 평가 (2주, ~40시간)

- [ ] 시상식 평가 기준 PDF 기반 평가 요소 설계
- [ ] `/branch` 인사평가 탭 실제 구현
- [ ] TO-DO 수행율 등 KPI 데이터 연동
- [ ] 3분기 데이터 기준 시뮬레이션 테스트

#### 5-4. TF 통계 + 경영 대시보드 (3주, ~60시간)

- [ ] TF별 통계: 특진병원별 재해자 카운팅, 기간별 문서 수발신, 최초접수건수 등
- [ ] 지사별 실적 대시보드: 약정건수, 승인율, 평균 처리 기간 시각화
- [ ] 경영 대시보드 기본 구현 (관리자/지사장 전용)

#### 5-5. 타 상병 확장 기획 (2주, ~40시간)

- [ ] COPD PRD 초안 작성 (근로복지공단 지침 수령 전제)
- [ ] 진폐 PRD 초안 작성 (근로복지공단 지침 수령 전제)
- [ ] 공통 구조(Patient → Case → 상병별 Detail) 확장 포인트 설계
- [ ] 일반산재 상병별 구조 초안

**Phase 5 완료 기준**:
- 직업력 산정 신뢰도 95%+ 달성
- 평균임금 로직이 실무 기준과 일치
- 연말 시상식 평가 시스템 가동 가능

---

### Phase 6 — RAG 자료검색 시스템 (2027.01 ~ 03)

> **목표**: 구글 드라이브 기반 자연어 검색 시스템을 구축한다.
> **예상 소요**: 약 10~12주 (총 200~240시간)
> **선행 조건**: 구글 드라이브 파일명·폴더 구조 규칙 통일 (직원 작업 완료), Phase 5 완료

#### 6-1. RAG-1: DB 구축 환경 설정 (1주)

- [ ] Railway PostgreSQL에 pgvector 익스텐션 활성화
- [ ] 구글 드라이브 서비스 계정 생성 + 폴더 공유
- [ ] Gemini 임베딩 API 설정
- [ ] document_chunks 테이블 생성 + 벡터 인덱스

#### 6-2. RAG-2: 구글 드라이브 연결 (2주)

- [ ] 서비스 계정 기반 드라이브 API 연동
- [ ] 폴더 탐색 + 파일 메타데이터 수집
- [ ] 신규 파일 감지 및 증분 인덱싱 자동화

#### 6-3. RAG-3: 승인·불승인 사례 검색 (3주)

- [ ] 상병별 승불자료 폴더 우선 인덱싱
- [ ] `/rag-search` 페이지 UI (검색창, AI 요약, 관련 파일 링크)
- [ ] 사건 상세 → "유사 사례 검색" 버튼 연동

#### 6-4. RAG-4~6: 확장 (4~6주)

- [ ] 지사 운영 자료 검색 (회의록, 업무지시 등)
- [ ] 지침·논문·정보 검색 + 이유서 작성 페이지 우측 패널 연동
- [ ] 전체 자료 통합 검색 + 텔레그램 봇 자연어 질의 연동

**Phase 6 완료 기준**:
- `/rag-search`에서 자연어 질의 → 관련 파일 + 드라이브 링크 반환
- 사건 상세 페이지에서 유사 사례 검색 가능

---

### 장기 (2027 하반기~)

- [ ] COPD / 진폐 실제 개발 (PRD 기반)
- [ ] 일반산재 페이지 개발 (근골격계 → 뇌심혈관 → 기타)
- [ ] 모바일 최적화 웹 (외근직 현장 입력용)
- [ ] 근로복지공단 공개 API 연동 (가능 시)
- [ ] 전체 SaaS화 검토

---

## 2. 페이지별 기획 상세

### 2-1. 로그인 페이지
- 상단: 대표 인사말(좌) + 관리자 공지사항(우) 박스
- 하단: 로그인 입력창 (가로로 넓게)
- [가입 요청] 버튼 → 관리자 페이지로 요청 전달
- 로그인 성공 → To Do List 이동

### 2-2. To Do List (로그인 후 첫 화면)
- 좌측 1/2: 달력 + 날짜별 To Do 이력
- 우측 상단: 요청 유형별 필터 (정산 요청, 이의제기 작성 요청, 제척 임박 등)
- 우측 하단: 기간 설정 조회
- 하단 여백: 자유 기술 메모
- 이의제기 기일 관리 → 제척 도래 시 자동 반영

### 2-3. 사건 관리
- 병원 환자 관리 프로그램 컨셉
- 재해자(Patient) 고정정보 기준 조회·관리
- 기획사건: 소음성 난청 / COPD / 진폐 (초진→특진→전문조사→장해급여)
- 일반산재: 근골격계, 업무상 사고, 직업성 암, 유족 등
- 스키마 상세 → `docs/기획서_개발.md` 섹션 3 참조

### 2-4. 사건 조회 (이산·외부)
- 대상: 이산 임직원 + 더보상 영업담당자 (본인 영업 사건만)
- 표시: 상병, 성명, 주민번호, 연락처, 진행경과, 담당 지사, TF, 노무사
- 목록 PDF 저장 기능

### 2-5. 이의제기 관리
- 기일 관리: 제척기간 산정·모니터링 → To Do List 자동 알림
- 이유서·의견서: 사건 선택 시 재해자 정보 자동 반영 → PDF 생성
- 우측 패널: 관련 사례·논문 (RAG-5 단계에서 구현)

### 2-6. TF 업무
- 담당TF 모니터링: 텔레그램방 webhook 연동
- TF 공지: 공지 서식 자동 작성 → 공지방 발송
- TF 통계: webhook 데이터 + 사건 DB 연계 시각화

### 2-7. 지사장 관리·운영 (조직관리자 전용)
- 접근: 조직관리자 이상
- 구성: 매출·비용, 외근직 실적, 내근직 업무 부하, 홍보 현황, 운영규정 조회, 업무 지시 게시판
- 인사 평가·승진 대상자 여부 판단 기능 포함
- 각 항목별 인입 DB 상세 → 상세 개발 시 별도 전달

### 2-8. 텔레그램 양방향 연동 상세 (Phase 4)

**텔레그램 → TBSS 방향**
| 기능 | 동작 방식 |
|------|----------|
| 키워드/패턴 감지 → DB 자동 저장 | webhook 수신 → 정규식 파싱 → DB INSERT |
| 메시지 → 사건 데이터 입력 트리거 | 특정 커맨드 감지 → TBSS 입력 폼 자동 열기 또는 API 호출 |

**TBSS → 텔레그램 방향**
| 기능 | 동작 방식 |
|------|----------|
| 기일/To Do 알림 자동 발송 | TBSS 크론잡 → 텔레그램 Bot API sendMessage |
| 사건 상태 조회 응답 | 봇 질의 → Claude API 자연어 처리 → DB 조회 → 응답 |

---

## 3. 현업 업무 방식 (현재 문제점 → 개선 방향)

| 현재 방식 | 문제점 | 개선 방향 |
|-----------|--------|-----------|
| NAS 엑셀로 사건 관리 | 파일 간 연결 없음, 동시 접속 서버 에러, 수작업 과다 | TBSS DB로 통합 |
| 텔레그램으로 문서 공유 | 파일 저장 불완전, 필터링 어려움 | webhook 양방향 연동 (Phase 4) |
| 한글 메일머지로 접수서식 생성 | 수작업, 파편화 | TBSS 자동 서식 생성으로 대체 |
| 통계 수작업 | 광역 통계 불가 | DB 기반 자동 집계·시각화 |

---

## 4. 미결 사항 (추후 전달 예정)

- [ ] 보험급여 청구서식 전체 목록 확보 + 우선순위 정리
- [ ] COPD/진폐 근로복지공단 지침 수령 시 PRD 초안 작성
- [ ] 인사 평가 기준 및 승진 판단 로직 상세 (Phase 5 착수 전)
- [ ] 구글 드라이브 파일명·폴더 구조 규칙 통일 (직원 작업, Phase 6 선행)

### 병행 작업 (Phase와 무관하게 수시)

- [ ] 매년 갱신 데이터 수기 입력 (최저임금, 건설노임단가, 보상기준금액)
- [ ] Railway 배포 안정성 모니터링 + Health Check
- [ ] CLAUDE.md 세션별 업데이트 유지

---

## 5. 주요 참고 데이터 파일

| 파일 | 내용 |
|------|------|
| 첨부1_더보상명단.xlsx | 전국 지사별 임직원 명단·연락처 |
| 첨부2_이산_명단.xlsx | 이산 법인 직원 명단 |
| 첨부3_근로복지공단_지사_현황.xlsx | 전국 공단 지사 목록 |
| 첨부4_전국_난청폐질환_초진병원_리스트.xlsx | 전국 초진 병원 목록 |
| 첨부5_전국_특진병원_리스트.xlsx | 전국 특진 병원 상세 정보 |
| 소음성난청_결과정리_Tool.xlsx | 특진 결과 입력/계산 툴 |
| 질병사인분류번호소분류_8차.xlsx | KCD 8차 질병 코드 |
| 노무법인_더보상_울산지사_전체_TF.xlsx | 울산지사 전체 사건관리 엑셀 (스키마 참조용) |
