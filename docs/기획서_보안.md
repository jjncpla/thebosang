# TBSS 보안기획서

> **담당 에이전트**: 보안
> **버전**: v2.0
> **최종수정**: 2026.04.15
> **목적**: 권한 매트릭스, 인증 흐름, 개인정보 처리, 접근 제어 규칙

---

## 1. 권한 체계

### 권한 Enum

| 권한 | 대상 | 설명 |
|------|------|------|
| `ADMIN` | 시스템 관리자 (이정준) | 전체 접근 + 관리자 페이지 + 계정 관리 |
| `조직관리자` | 대표, 이사, 권역지사장, 지사장, 센터장, 연구원장, 법률원장, 수석노무사 | 지사장 관리·운영 페이지 포함 |
| `STAFF` | 노무사, 내근직, 외근직 | 일반 업무 기능 전체 |
| `이산계정` | 노무법인 이산 임직원 | 사건 조회 페이지만 접근 |

### 계정 생성 방식
- 자발적 가입 불가
- 사용자가 가입 요청 → ADMIN이 계정 생성 후 전달
- 계정 형식: `이니셜3자리+입사순번@thebosang.kr`

---

## 2. 페이지별 접근 권한 매트릭스 (2026.04.15 기준)

| 페이지 | ADMIN | 조직관리자 | STAFF | 이산계정 | 비로그인 |
|--------|-------|-----------|-------|---------|---------|
| 로그인 `/login` | ✅ | ✅ | ✅ | ✅ | ✅ |
| To Do List `/todo` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 사건 목록 `/cases` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 사건 상세 `/cases/[caseId]` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 사건 신규 `/cases/new` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 사건 임포트 `/cases/import` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 사건 DB `/cases/db` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 상담 관리 `/consultation` | ✅ | ✅ | ✅ | ❌ | ❌ |
| **사건 조회** `/cases-view` | ✅ | ✅ | ✅ | ✅ (본인 영업만) | ❌ |
| 재해자 관리 `/patients` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 처분 검토 `/objection/review` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 기일 관리 `/objection/deadline` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 이유서·문서 `/objection/document` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 평균임금 검토 `/objection/wage-review` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 보험급여 서식 `/forms` | ✅ | ✅ | ✅ | ❌ | ❌ |
| TF 특진 일정 `/tf/special-clinic` | ✅ | ✅ | ✅ | ❌ | ❌ |
| TF 모니터링 `/tf-monitor` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 장해등급/평균임금 `/grade`, `/wage` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 법령 및 규정 `/law` | ✅ | ✅ | ✅ | ❌ | ❌ |
| 자료 검색 (Phase 6) | ✅ | ✅ | ✅ | ❌ | ❌ |
| **지사장 관리·운영** `/branch` | ✅ | ✅ | ❌ | ❌ | ❌ |
| **관리자 페이지** `/admin/*` | ✅ | ❌ | ❌ | ❌ | ❌ |
| **관리자 임포트** `/admin/import` | ✅ | ❌ | ❌ | ❌ | ❌ |
| **담당자 매핑** `/api/admin/map-case-managers` | ✅ | ❌ | ❌ | ❌ | ❌ |

---

## 3. 데이터 접근 제한

### 이산계정 제한
- 사건 조회 페이지: 요약 정보만 표시 (상병, 성명, 주민번호, 연락처, 진행경과, 담당 지사, TF, 노무사)
- 사건 상세 페이지: **접근 불가** (라우팅 차단)
- 본인 영업 사건만 조회 가능 (TF 필터 자동 적용)

### STAFF 제한
- 지사장 관리·운영 페이지: 접근 불가
- 다른 지사 민감 데이터 조회 제한 (추후 구현)

### 상병별 상세페이지 작업 시 권한 체크 (Phase 5-6)

> 각 상병 상세페이지 작업 시 본 항목을 추가 점검. 상세는 `docs/기획서_기획.md` §5-6 참조.

- [ ] **이산계정 caseType 필터링** — MUSCULO / OCC_ACC / PNEUMO / OCC_CAN / BEREAVED 신규 작업 시 이산계정의 caseType별 노출 정책 확인 (현재 이산은 영업담당자 본인 사건만 조회 가능 + 사건 상세 차단)
- [ ] **자동연계 sync 함수 권한 체크** — `syncFrom[X]Decision` 호출 API에 `auth()` 세션 검증 + role 분기 누락 없는지 확인
- [ ] **ObjectionReview / ObjectionCase 자동 인입 시 권한 격리** — 자동 생성된 row가 다른 지사 STAFF에게 노출되지 않는지 검증

### 주민번호 처리
- DB 저장: 전체 주민번호 저장
- 화면 표시: 기본 마스킹 (`000000-*******`)
- 마스킹 해제: ADMIN / 조직관리자 / STAFF만 가능 (이산계정 불가)
- API 응답: 이산계정 요청 시 주민번호 마스킹 처리 필수

---

## 4. 인증 흐름 (Auth.js v5)

```
사용자 로그인 요청
  → POST /api/auth/signin
  → Auth.js v5 (NextAuth) 세션 생성
  → 세션 쿠키 발급

페이지 접근
  → middleware.ts 에서 세션 확인
  → 권한 체크 (role 필드)
  → 권한 없으면 /login 또는 /unauthorized 리다이렉트

API 접근
  → auth() 호출로 세션 확인
  → session.user.role 기반 분기
```

### 중요 설정
- `AUTH_URL` 환경변수: Railway 프로덕션 URL로 반드시 설정
  - 미설정 시 로그아웃이 localhost로 리다이렉트되는 버그 발생
- 세션 만료: 기본값 사용 (추후 조정 필요 시 협의)

---

## 5. API 보안 규칙

### 모든 API 라우트 필수 처리
```typescript
// 모든 API Route에서 세션 확인 필수
const session = await auth()
if (!session) return Response.json({ error: 'Unauthorized' }, { status: 401 })

// 권한별 분기 예시
if (session.user.role === '이산계정') {
  // 이산계정 제한 처리
}
```

### 이산계정 API 응답 처리
```typescript
// 주민번호 마스킹 필수
const maskedPatient = {
  ...patient,
  ssn: session.user.role === '이산계정'
    ? patient.ssn.replace(/(\d{6})-?(\d{7})/, '$1-*******')
    : patient.ssn
}
```

---

## 6. 텔레그램 Webhook 보안

### 수신 검증
```typescript
// Telegram-Bot-Token 헤더 또는 secret token 검증
const telegramToken = req.headers.get('X-Telegram-Bot-Api-Secret-Token')
if (telegramToken !== process.env.TELEGRAM_WEBHOOK_SECRET) {
  return Response.json({ error: 'Forbidden' }, { status: 403 })
}
```

### 환경변수
- `TELEGRAM_BOT_TOKEN`: 봇 API 토큰 (Railway 환경변수)
- `TELEGRAM_WEBHOOK_SECRET`: webhook 검증용 시크릿
- 절대 클라이언트 코드에 노출 금지

---

## 7. 구글 드라이브 연결 보안 (Phase 6)

### 접근 방식
- 구글 서비스 계정 기반 연동 (OAuth 불필요)
- 인덱싱 전용 읽기 전용 권한 (뷰어 공유)
- 환경변수 관리:
  - `GOOGLE_SERVICE_ACCOUNT_KEY`: 서비스 계정 JSON 키 (Railway 환경변수)
  - `GOOGLE_DRIVE_FOLDER_ID`: 인덱싱 대상 루트 폴더 ID
  - `GEMINI_API_KEY`: Gemini 임베딩 API 키

---

## 8. 개인정보 처리 원칙

### 민감 정보 목록
- 주민등록번호 (전체)
- 연락처
- 주소
- 진단명 / 장해등급 / 보상금액

### 처리 원칙
1. 최소 수집: 업무에 필요한 정보만 수집
2. 접근 제한: 권한에 따른 데이터 노출 수준 차별화
3. 이산계정: 상세 의료 정보 / 보상금액 노출 불가
4. 로그: 민감 데이터 조회 로그 추후 구현 검토

---

## 9. ADMIN 전용 보안 관련 API

| API | 기능 | 권한 체크 |
|-----|------|-----------|
| `/api/admin/map-case-managers` GET | 담당자 미매핑 현황 조회 | ADMIN |
| `/api/admin/map-case-managers` POST | TF별 자동매핑 / 수동매핑 | ADMIN |
| `/api/admin/match-contacts-users` POST | Contact→User 자동 연결 | ADMIN |
| `/api/admin/import-hearing-loss` POST | 소음성난청 벌크 임포트 | ADMIN |
| `/api/admin/import/consultation` POST | 상담 데이터 임포트 | ADMIN |
| `/api/admin/import/objection-*` POST | 이의제기 데이터 임포트 | ADMIN |
| `/api/admin/delete-*` DELETE | 데이터 일괄 삭제 | ADMIN |
| `/api/admin/users` CRUD | 사용자 관리 | ADMIN |
| `/api/admin/signup-requests` CRUD | 가입 요청 관리 | ADMIN |

---

## 10. 환경변수 목록 (보안 관련)

| 변수명 | 용도 | 비고 |
|--------|------|------|
| `AUTH_SECRET` | Auth.js 세션 암호화 | 필수 |
| `AUTH_URL` | 프로덕션 URL | Railway URL 설정 필수 |
| `DATABASE_URL` | 메인 PostgreSQL 연결 | Railway 자동 주입 |
| `DATABASE_AUTH_URL` | Auth DB 연결 | 별도 Prisma Client |
| `TELEGRAM_BOT_TOKEN` | 텔레그램 봇 | 구현 완료 |
| `TELEGRAM_WEBHOOK_SECRET` | webhook 검증 | 구현 완료 |
| `ANTHROPIC_API_KEY` | Claude API (직업력 산정, 텔레그램 봇) | 구현 완료 |
| `DATA_PORTAL_API_KEY` | 공공데이터 포탈 (공휴일 API) | 구현 완료 |
| `GOOGLE_SERVICE_ACCOUNT_KEY` | 구글 드라이브 서비스 계정 | Phase 6 |
| `GOOGLE_DRIVE_FOLDER_ID` | 인덱싱 대상 폴더 | Phase 6 |
| `GEMINI_API_KEY` | Gemini 임베딩 API | Phase 6 |
