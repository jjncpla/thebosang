# 작업 이력 — 2026-04-30 야간 세션 (~05-01 새벽)

> 04-29~30 본 작업의 야간 후속. Phase B-5 (3종 상병 UI) + UI 디버깅 + 결정통지서 OCR 비교 + 학습 보충 병렬 진행.

## 요약

| 영역 | 결과 |
|------|------|
| Phase B-5 UI 폼 | 근골격계/업무상사고/직업성암 3종 상세+목록+API+사이드바 + 스키마 9 필드 추가 |
| UI 디버깅 | `FormModal` 신규, `prompt()` 11회 제거, 이의제기 양식 4종 한글·에러 통일 |
| 결정통지서 OCR | `lib/decision-notice-parser.ts` 신규 (18 필드 + 자동 인입 판정), 비교 보고서 추가 |
| 학습 보충 | retry_ocr 285/290, OneDrive 6 + Z 드라이브 6개 폴더 추가 인덱싱 (3,222건) |
| 분류 결과 | 47종 → **51종**, 학습 텍스트 3,301 → **3,586건** |

---

## 1. Phase B-5 — 6개 미구현 상병 중 나머지 3종 (code 에이전트)

### 1.1 신규 페이지 (9개)
```
/cases/musculoskeletal                    근골격계 사건 목록
/cases/[caseId]/musculoskeletal           근골격계 상세
/cases/occupational-accident              업무상사고 사건 목록
/cases/[caseId]/occupational-accident     업무상사고 상세
/cases/occupational-cancer                직업성암 사건 목록
/cases/[caseId]/occupational-cancer       직업성암 상세
```

### 1.2 신규 API (3개, GET/PATCH)
```
/api/cases/[caseId]/musculoskeletal
/api/cases/[caseId]/occupational-accident
/api/cases/[caseId]/occupational-cancer
```

### 1.3 Prisma 스키마 변경 (+9 필드)
- `MusculoskeletalDetail`: `injuryDescription`, `restTimePattern`, `qualityReviewStatus`, `concurrentDiseases (Json)`
- `OccupationalAccidentDetail`: 동일 4 필드
- `OccupationalCancerDetail`: `cancerType`, `occupation`, `exposureSubstance`, `exposurePeriod`, `concurrentDiseases (Json)`
- 모두 `String?`/`Json?` (선택형) → 기존 데이터 마이그레이션 불필요

### 1.4 사이드바 추가
`components/AppShell.tsx` 51-53 라인 — "사건 관리" 그룹, 유족급여 사건 아래에 3개 추가.

### 1.5 ⚠️ 사용자 액션 필요
```bash
npx prisma db push
```
(Railway production DB 적용. 자동 실행 차단됨 — 사용자 직접 실행 필요.)

### 1.6 후속 (미완료)
- `/cases/new` 등록 UI에 caseType 옵션 노출 검증
- 6개 상병별 PDF 자동생성 (요양/휴업/장해)
- caseType 필터 검토 (objection/review, wage/avg-wage)

---

## 2. UI 디버깅 — `prompt()` → 모달 (code 에이전트)

### 2.1 신규
- `components/ui/FormModal.tsx` — 재사용 가능 모달 (외부 패키지 0개)
  - field type: `text | textarea | date | number | select`
  - ESC + 오버레이 클릭 닫기, 필수 검증 내장
  - 한국어 라벨/placeholder 정상 렌더링

### 2.2 변경
- `app/wage/avg-wage/page.tsx` — `window.prompt()` 11회 호출 제거 → FormModal 2개 (정정청구서 PDF 입력 / WageReviewData 변환)
- 이의제기 양식 4종 (`exam-claim`, `reexam-claim`, `additional-injury`, `requote`) — 한글 placeholder + 필수 필드 빨간 테두리 + 에러 메시지 통일

### 2.3 백엔드 호환성
- 기존 fetch 페이로드 1:1 매핑 → 백엔드 변경 없음

---

## 3. 결정통지서 OCR 정확도 비교 (ocr-parser 에이전트)

### 3.1 핵심 발견
- 723건 결정통지서 표본 분석
- **`txt/`와 `ocr/` 둘 다 가능한 케이스 0건** — 결정통지서는 거의 100% 스캔 PDF (FAX 출력본)
- Document AI 단독 권장 (Tesseract 폴백 불필요)

### 3.2 신규 산출물
- `lib/decision-notice-parser.ts`
  - `parseResolutionNotice(rawText): ParsedResolutionNotice` — 18 필드
  - `evaluateAutoIngest(parsed)` — HIGH/MEDIUM/LOW + canAutoIngest + requiresUserReview
  - `DiseaseCategory` 분류기 (HEARING_LOSS / PNEUMOCONIOSIS / COPD / LUNG_CANCER / MUSCULOSKELETAL / GENERAL / UNKNOWN)
  - `ResolutionStatus` 분류기 (APPROVED / REJECTED / PARTIAL / UNKNOWN)
  - 기존 `lib/notice-parser.ts` (지급 결정통지서)와 분리 — 본 파서는 **요양급여 결정통지서** 전용
- `docs/forms-learning/결정통지서_OCR_정확도_비교.md`
  - 12 핵심 필드 추출 성공률 표
  - 자동 인입 권장 5 필드 (HIGH): 결정일·공단지사·우편번호·관리번호·승인/불승인 마커
  - 사용자 확인 필수 4 필드 (MEDIUM): 재해자명·상병명·의료기관·요양기간
  - 수기 입력 권장 2 필드 (LOW): 불승인 사유 본문·산정내역
  - 306건 OCR 표본 검증: APPROVAL 79%, PNEUMOCONIOSIS 79% 자동 인입 가능

### 3.3 후속
- Document AI 실제 호출 검증 (표본 5건)
- `ResolutionNotice` Prisma 모델 신설 (Phase B-7)
- HEARING_LOSS_DECISION 분류기 보강 (정보공개결정통지서·심사결정서·팩스 표지 false-positive)

---

## 4. 학습 보충 (백그라운드)

### 4.1 retry_ocr (290건 → 285 성공, 98%)
- Phase 3 후반 leptonica 메모리 누수로 실패한 290건 retry
- 1워커 + 100건마다 5초 휴식 → 안정화
- 약 3시간 처리, 5건만 잔존 실패 (깨진 PDF로 추정)

### 4.2 추가 인덱싱 (OneDrive 6 + Z 드라이브 7개 폴더)
- 초기 인덱싱 393,306개 파일 → **zdrive_최초요양신청 폴더 389,946개 제외** (사건별 자료 묶음, 양식 학습 ROI 낮음) → 3,222개 양식 후보
- 처리 결과:
  - text (직접 추출): **552건** ★
  - scan_or_empty (OCR 필요): 2,627건
  - empty: 13건
  - error: 30건
- by_root: onedrive_6 327, zdrive_정보공개서류 621, zdrive_의무기록 1937, 산재자료 129, 진폐tf 18, 지침처리요령 31, 재해보상법률원 159

### 4.3 분류 + 필드 추출 + 보고서 묶음 재실행
- classify_v2: **47종 → 51종** (신규 4종)
- 학습 가용 텍스트: 3,301 → **3,586건** (retry 285 추가)
- bundle_for_llm.md 갱신 (343KB → 347KB)

### 4.4 신규 인프라
- `index/build_index_extra.py` — OneDrive 6 + Z 드라이브 인덱싱
- `index/filter_index_extra.py` — 거대 root 제외 필터
- `index/extract_text_extra.py` — file_index_extra_filtered.jsonl 전용 추출
- `index/watchdog.py` — 60분 stuck 자동 재시작 (utf-8 encoding fix)
- `index/retry_ocr.py` — 메모리 누수 회피 retry 큐

### 4.5 미완료 (우선순위 결정 대기)
- **scan_or_empty 2,627건 OCR**: 38초/건 × 2,627 = 약 28시간 예상. 우선순위 큐 (양식 키워드 필터) 또는 Document AI 배치로 가동 필요.
- 신규 양식 5종 사용 후 OCR 정확도 검증 (사용자 사용 후)

---

## 5. ⚠️ 사용자 액션 필요

1. **`npx prisma db push`** — Railway DB에 `MusculoskeletalDetail`/`OccupationalAccidentDetail`/`OccupationalCancerDetail` 신규 9 필드 적용 (메인 디렉토리에서 실행)
2. **scan_or_empty 2,627건 OCR 가동 여부 결정** — 28시간 예상. 우선순위 필터(양식 키워드) 적용 시 단축 가능
3. **배포 후 검증**:
   - `/cases/musculoskeletal`, `/cases/occupational-accident`, `/cases/occupational-cancer` 정상 표시
   - `/wage/avg-wage` 정정청구서 PDF / 변환 모달 동작
   - `/forms/objection/*` 4종 양식 한글 placeholder + 검증 동작

---

## 부록: 파일 인벤토리 (변경)

### 신규 (13개)
```
app/api/cases/[caseId]/musculoskeletal/route.ts
app/api/cases/[caseId]/occupational-accident/route.ts
app/api/cases/[caseId]/occupational-cancer/route.ts
app/cases/[caseId]/musculoskeletal/page.tsx
app/cases/[caseId]/occupational-accident/page.tsx
app/cases/[caseId]/occupational-cancer/page.tsx
app/cases/musculoskeletal/page.tsx
app/cases/occupational-accident/page.tsx
app/cases/occupational-cancer/page.tsx
components/ui/FormModal.tsx
lib/decision-notice-parser.ts
docs/forms-learning/결정통지서_OCR_정확도_비교.md
docs/작업이력_2026-04-30_야간세션.md (본 문서)
```

### 수정 (8개)
```
prisma/schema.prisma                              +9 필드
components/AppShell.tsx                           +3 사이드바 메뉴
app/api/cases/route.ts                            select 보강
app/wage/avg-wage/page.tsx                        prompt 제거 + 모달 2개
app/forms/objection/exam-claim/page.tsx           검증·한글 통일
app/forms/objection/reexam-claim/page.tsx         동일
app/forms/objection/additional-injury/page.tsx    동일
app/forms/objection/requote/page.tsx              동일
```
