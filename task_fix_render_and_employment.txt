## 작업 개요
두 가지 수정:
1. 분석 완료 후 새로고침 없이 즉시 데이터 표시
2. 고용보험 PDF 하나에서 상용직/일용직 자동 구분

## 수정 파일 3개
- app/patients/[patientId]/page.tsx
- components/case-common/WorkHistorySection.tsx
- app/api/cases/[caseId]/work-history/analyze/route.ts

---

## 작업 1: page.tsx - handleWorkHistoryChange 즉시 반영으로 수정

CaseCommonInfoSection 함수 내부의 handleWorkHistoryChange 함수를 찾아서 아래로 교체.

기존 패턴 (await fetch 후 onUpdated):
```typescript
const handleWorkHistoryChange = async (updates: {...}) => {
  setWorkHistorySaving(true)
  try {
    await fetch(...)
    onUpdated({ ...caseItem, ...updates })
  } ...
}
```

교체 내용 - onUpdated를 fetch 전에 먼저 호출:
```typescript
const handleWorkHistoryChange = async (updates: {
  workHistory?: WorkHistoryItem[] | null
  workHistoryRaw?: WorkHistoryRaw | null
  workHistoryMemo?: string | null
  lastNoiseWorkEndDate?: string | null
}) => {
  // 즉시 UI 업데이트 (낙관적 업데이트)
  onUpdated({ ...caseItem, ...updates })
  try {
    await fetch(`/api/cases/${caseItem.id}`, {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(updates),
    })
  } catch (e) {
    console.error("직업력 저장 실패:", e)
  }
}
```

마찬가지로 onChangeDaily 핸들러도 즉시 반영으로 수정:
```typescript
onChangeDaily={async (entries) => {
  // 즉시 UI 업데이트
  onUpdated({ ...caseItem, workHistoryDaily: entries })
  try {
    await fetch(`/api/cases/${caseItem.id}`, {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ workHistoryDaily: entries }),
    })
  } catch (e) {
    console.error("일용직 저장 실패:", e)
  }
}}
```

---

## 작업 2: WorkHistorySection.tsx - DOC_TYPE_OPTIONS에 고용보험 통합 옵션 추가

DOC_TYPE_OPTIONS 배열에서 기존 고용보험 두 항목을 아래로 교체:

기존:
```typescript
{ value: "고용산재_상용", label: "고용보험 자격이력내역서 (상용직)" },
{ value: "일용직", label: "고용보험 일용근로노무제공내역서" },
```

교체:
```typescript
{ value: "고용산재_전체", label: "고용보험 (자격이력 + 일용근로 통합)" },
{ value: "고용산재_상용", label: "고용보험 자격이력내역서 (상용직만)" },
{ value: "일용직", label: "고용보험 일용근로노무제공내역서 (일용직만)" },
```

---

## 작업 3: route.ts - 고용산재_전체 프롬프트 추가

getPromptForDocType 함수에서 고용산재_상용 케이스 바로 위에 추가:

```typescript
if (docType === "고용산재_전체") {
  return base + `이 문서는 고용보험 자료로, 자격이력내역서(상용직)와 일용근로노무제공내역서(일용직)가 함께 포함되어 있을 수 있다.

[상용직 추출 규칙]
- "고용보험 자격이력내역서" 또는 "자격이력내역서(근로자용/피보험자용)" 섹션에서 추출
- 비고가 "근로자"인 항목을 모두 추출
- 직종명(코드), 사업장명, 취득일/전근일(시작), 상실일(종료)을 추출
- 상실일이 없으면 현재(2026-01)로 표기

[일용직 추출 규칙]
- "일용근로노무제공내역서" 섹션에서 추출
- 문서에 나와있는 모든 행을 그대로 추출. 합산하거나 묶지 마라
- 각 행의 사업장명, 직종명, 근무일수를 추출
- 사업장명에 [업체명] 형태가 있으면 [] 안의 업체명을 company로 사용
- 근무일수는 비고란의 날짜 숫자들의 개수를 세어라. 예: '1,2,3,4,5' → 5일
- startYear/startMonth는 해당 행의 연월
- convertedMonths = Math.ceil(totalDays / 20)

JSON 형식:
{
  "name": "성명",
  "sources": {
    "고용산재": [{ "company": "사업장명", "startYear": 2000, "startMonth": 1, "endYear": 2005, "endMonth": 12, "department": "", "jobType": "직종명(코드)", "workDays": 0 }],
    "건보": [], "소득금액": [], "연금": []
  },
  "dailyEntries": [
    { "company": "사업장명", "jobType": "직종명", "totalDays": 5, "startYear": 2013, "startMonth": 1, "convertedMonths": 1, "source": "고용산재", "memo": "" }
  ]
}`
}
```

---

## 완료 후 확인
1. PDF 분석 후 새로고침 없이 탭에 데이터 즉시 표시
2. 고용보험 통합 옵션으로 PDF 1개 업로드 시 상용직은 고용산재 탭, 일용직은 일용직 합산표에 동시 반영
3. git push
