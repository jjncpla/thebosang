# Telegram session files = full account access. NEVER commit.
*.session
*.session-journal

# Credentials
.env

# Downloaded attachments (local cache only)
downloads/

# Learning corpus — chat messages + media archives. Contains PII.
corpus/

# Dialog dumps / whitelist drafts — chat room metadata can be sensitive
dialogs_dump.json
telegram-whitelist.draft.json
fetch_plan.json
*.local.json

# Python
__pycache__/
*.pyc
.venv/
venv/
